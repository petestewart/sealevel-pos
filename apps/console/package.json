{
  "name": "@ai-manager/console",
  "version": "0.0.1",
  "private": true,
  "description": "Operator console: Next.js (App Router) + Clerk, widget registry, approval inbox.",
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "tsc -b ../../packages/core && NODE_ENV=production next build",
    "start": "next start",
    "typecheck": "tsc -b ../../packages/core && tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf .next"
  },
  "dependencies": {
    "@ai-manager/core": "*",
    "@clerk/nextjs": "^6.39.5",
    "next": "^15.5.20",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3"
  }
}
