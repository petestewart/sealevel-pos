/**
 * Worker app — heartbeat + BullMQ workers + brain (always-on).
 * Skeleton placeholder: queue and brain wiring land in later Phase 0 tickets.
 */
console.log("ai-manager worker skeleton: nothing to run yet");
