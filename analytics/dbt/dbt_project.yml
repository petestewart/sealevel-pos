# dbt scaffold for the AI Manager analytics stack (ARCHITECTURE.md "Analytics stack").
# Sources, staging, and marts land with the Phase 2 analytics tickets.
name: ai_manager
version: "0.0.1"
profile: ai_manager
model-paths: ["models"]
test-paths: ["tests"]
models:
  ai_manager:
    staging:
      +materialized: view
    marts:
      +materialized: table
