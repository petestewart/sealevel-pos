/**
 * @ai-manager/features — vertical feature slices (email, social, analytics, expenses).
 * Skeleton placeholder: feature modules land in later tickets.
 */
export const FEATURES_PACKAGE = "@ai-manager/features";
