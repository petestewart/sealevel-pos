/**
 * @ai-manager/core — shared core (brain, queue, tools, jobs, db).
 * Skeleton placeholder: real modules land in later Phase 0 tickets
 * (see ARCHITECTURE.md "Repo structure").
 */
export const CORE_PACKAGE = "@ai-manager/core";
